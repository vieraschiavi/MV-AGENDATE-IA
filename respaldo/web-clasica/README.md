# Respaldo — workspace clásico (HTML + JS vanilla)

Versión anterior del panel de gestión (panel/agenda/clientes/dashboards/ayuda)
en HTML estático + JS vanilla, previa a la migración a React (webapp/).

Para restaurarla: copiá estos .html a public/ (los actuales son redirects a
/app/) — el CSS y JS compartidos (public/css/workspace.css y
public/js/workspace.js) siguen en su lugar, así que funcionan tal cual.
